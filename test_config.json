{
  "target_note": "Base directory where tests are created",
  "target": "./tests/",

  "test-families" : {
  
    "sgd": {
      "primes": ["p0", "p1", "p2", "p3", "p4"],
      "size_notes": "N samples, N features",
      "min_size": 2,
      "max_size": 5
    }

  }
}
